contract C1 {
    // this is a comment
    int count;
    bool b;
  
    function f(x) { skip }

    function pay(?x:T,y) {
        require (count<MAX_COUNT);
        if (sender==0) { count=count+1; s="ciao" }
        else { sender!balance:T }
    }

    function g(x) { skip }
    function h() { skip }    
}
